# MVP Product Requirements

## Outcome

用户输入公开的 Steam `/profiles/<steamid64>` URL、Steam ID64 或 Dota Account ID 后，可以查看最近 100 场已导入比赛、英雄使用统计和比赛详情，并查询当前版本英雄、物品与地图百科。Vanity URL 需要额外 Steam API 能力，不属于首版。

## P0 journeys

1. 账号查询 → 同步状态 → 玩家概览。
2. 玩家概览 → 英雄使用列表 → 玩家英雄详情。
3. 玩家概览 → 最近比赛 → 单场比赛详情。
4. 比赛中的英雄/物品 → 百科详情。
5. 导航 → 当前地图 → 版本化地点说明。

当前实现优先级调整为：地图真实快照暂缓，先完成英雄与物品百科第一阶段。该阶段只包含当前官方静态资料、结构化效果与最近官方版本变更；英雄胜率、物品购买率、出装和其他比赛聚合属于后续第二阶段。

英雄目录必须遍历游标并显示当前快照中的全部英雄，不得把 API 单页上限描述成英雄总数。默认采用 Dota 当前四种官方主属性——力量、敏捷、智力、全才——分组；全才英雄不得被错误塞入前三类。Web 可提供仅保存在当前浏览器中的自定义英雄分组，不把本地偏好伪装成账号云同步。

物品百科只面向当前普通比赛中有正向可见性证据的商店物品、当前中立物品与当前中立附魔。图纸、旧版本遗留、活动、测试、英雄专属和其他内部定义不进入目录或详情路由；“官方 datafeed 中仍有定义”不是当前可见性的充分证据。过滤必须发生在数据源标准化与快照替换边界，不能只靠前端隐藏。

## P0 pages

- 账号查询与同步页。
- 玩家概览页。
- 玩家英雄详情页。
- 比赛详情页。
- 英雄列表与详情页。
- 物品列表与详情页。
- 当前地图百科页。
- 数据状态页：展示整体/provider 状态、检查时间、来源和最新数据水位；未定义口径前不展示伪造的全局成功率。

## Account statistics

- 已导入比赛数、胜负与胜率。
- 英雄使用场次、使用占比、胜率。
- 总量 KDA 与场均 K/D/A。
- 数据存在时显示 GPM、XPM、补刀和英雄伤害。
- 完整比赛详情优先展示十人阵容、等级、GPM/XPM、补刀/反补、伤害、最终装备与技能升级顺序。
- 购买/出售时间线只有在上游或 replay 提供真实事件时展示；不可用状态不得退化为空时间线。
- 最近 20/50/100 场；不足时显示实际样本量。
- 所有列表稳定排序：`start_time DESC, match_id DESC`。

## Explicit non-goals

- 私密比赛历史解锁或绕过。
- 完整职业生涯承诺。
- 精确购买时间、技能加点时间线和每分钟曲线。
- 眼位、死亡、移动、团战热图和推荐路线。
- 修正后的克制/配合模型。
- React Native、GraphQL、公开开发者 API 和自研 replay parser。

## Acceptance

- 一个公开账号可以幂等同步最近 100 场比赛。
- 玩家总场次、英雄汇总和比赛列表可逐场对账。
- 同一比赛重复导入不重复计数。
- 私密、部分数据、限流、上游异常和失败有独立状态。
- 当前版本英雄、物品可查询；地图只有在来源、Patch 和 geometry 均已验证时才返回，否则明确返回 `MAP_UNAVAILABLE`。
- 地图快照必须可追溯到指定 Steam App 570 build/depot/resource hash 与提取器版本；不使用官方 minimap 纹理，不手写坐标。未取得合法内容快照与用途许可时，生产继续显示不可用状态。
- 物品定义必须区分普通物品、配方、中立物品与中立强化项；定义存在但当前可购买性未验证时明确显示 `unverified`。
- 英雄详情必须展示当前官方版本、基础属性、定位、技能说明与结构化技能数值；物品详情必须展示价格、类别、效果数值与合成组件。缺失字段显示 unavailable，不从比赛样本推断规则数据。
- 英雄和物品详情必须展示最近 5 个已同步官方版本中与该实体直接关联的变更。没有匹配项与更新目录 partial 必须区分，变更文本保留官方版本、发布日期和来源链接。
- 每个统计值带样本范围、覆盖率、来源、版本和更新时间。
- 冻结黄金数据集可重复计算出完全相同的结果。
