import type {
  ErrorMeta,
  OperationMeta,
  PlayerProfile,
  ResponseMeta,
} from "@dodo/contracts";

export type { ErrorMeta, OperationMeta, ResponseMeta };
export type PlayerDataStatus = PlayerProfile["status"];
